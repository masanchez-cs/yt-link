# Opcional: URL pública del API si el front se sirve en otro dominio (S3/CloudFront).
# Sin barra final. En local con `npm run dev` déjalo vacío (usa el proxy de Vite).
# Ejemplo producción: VITE_API_BASE=https://api.tudominio.com
VITE_API_BASE=
